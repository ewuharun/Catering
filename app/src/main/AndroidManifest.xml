<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.catering" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Catering"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".Customer.CustomerMainActivity"
            android:exported="true" />
        <activity
            android:name=".Registration.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".Registration.SendOtpActivity"
            android:exported="true" />
        <activity
            android:name=".Registration.LoginByPhoneActivity"
            android:exported="true" />
        <activity android:name=".Registration.VerifyPhoneActivity" />
        <activity android:name=".Registration.CustomerRegistrationActivity" />
        <activity android:name=".Registration.SelectUser" />
        <activity android:name=".Registration.RegistrationActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>